#!/usr/bin/env bash
# After checkout, re-apply local-only S3 secrets for Make publishing.
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
python3 "$ROOT/scripts/apply-local-s3-secrets.py" || true
