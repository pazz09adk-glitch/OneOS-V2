---
description: 台账三视角（列表/看板/主从）按业务实用性取舍，禁止机械全页强制；须在 PRD 写明视图决策
globs: src/prototypes/**/*
alwaysApply: true
---

# OneOS V2 · 三视角按业务取舍（强制）

`DESIGN.md` 与台账母版中的 **列表 / 看板 / 主从** 是**默认菜单与组件能力**，**不是**「凡台账必须三块都上」。  
Agent **禁止**以「规范写了三视角」为由，给不适用页面硬塞看板或主从。

## 1. 判定顺序（先业务，后控件）

1. 用一句话写清页面主任务（谁、一天几单、怎样算办完）。  
2. 对每个视角过「上岗门槛」；**门槛未过则不上**（或二期再议）。  
3. 在 `.spec/requirements-prd.md` 写 **「视图决策」**（做了哪些、**明确不做哪些 + 理由**）。  
4. 未上的视角：**不要**在顶栏留空洞的 `V2SegmentedControl` 占位。

## 2. 上岗门槛（摘录）

| 视角 | 何时适合 | 常见否决 |
|---|---|---|
| **列表** | 扫账、对账、多字段对比、筛选排序后进详情 | —（台账底盘，通常必有） |
| **看板** | 列 = **真实业务阶段**，且存在「推进/拖列改态」 | 列只是筛选项别名（如未关联/部分/已闭环）；卡片塞不下决策字段 |
| **主从** | 高频连续办多单，右栏能完成**主要**办理 | 真正活在重详情/表单全页（智能匹配、多段表单）；主从右栏只会变成半成品分屏 |

**详情 / 表单全页**（§4.8 / §4.9）是另一套布局，**不要**用三视角顶栏冒充详情页头。

## 3. 与视觉对齐规则的关系

- **仍要对齐**：台账连体、Pill Tabs（带 count）、筛选去重、表格密度、`V2Button` / Token、详情页头等（见 `oneos-v2-prototype-visual-align`）。  
- **顶栏形态**：  
  - 多视角时：`V2SegmentedControl` + 主操作；  
  - **仅列表时**：状态/分类 Pill Tabs + 主操作即可，**不强行**假三视角切换。  
- 审计时若「缺看板/主从」，先读本页 PRD「视图决策」；有合理不做理由 → **不算视觉不合规**。

## 4. 示例口径（收付款中枢类）

> 主任务：流水认领核销与结清门禁。  
> **视图决策**：仅列表 + 详情全页。看板不做（关联状态是闭合进度非流程阶段）。主从不做（智能匹配与多单拆分在详情完成，主从与详情重复）。

同类「导入/认领/核销/对账」页可复用此口径，勿机械抄租赁合同三视角母版。
