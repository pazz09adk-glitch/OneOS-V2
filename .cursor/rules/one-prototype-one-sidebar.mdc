---
description: 一个原型只对应侧栏一项；默认单页内切换，禁止默认拆列表/详情下级页
alwaysApply: true
---

# 原型侧栏：一原型一项（强制偏好）

产品偏好：**一个原型目录 = Make 侧栏一个入口**，不在侧栏下再挂「列表 / 详情 / 新建」等下级页。

## 默认做法

- 列表、看板、主从、详情、弹窗、抽屉等，一律在**同一页内用状态切换**模拟（参考 `lease-contract-management`、已收拢的 `vehicle-management`）。
- **不要**默认使用 `defineHashPageRoute` / `useHashPage` 拆出多个 `#page=`，以免侧栏出现套娃文件夹。
- `annotation-source.json` 的 `directory` **不要**为同一原型挂多个 `type: "route"` 的 list/detail 下级项；说明文档用 markdown 节点即可。

## 何时才允许多页面 hash

仅当用户**明确要求**以下任一情形时再拆 `#page=`：

- 需要侧栏直达某一独立流程页（如独立「新建合同」且用户要单独入口）
- 需要可分享的固定 URL（`#page=xxx`）做验收/外发
- 用户书面指定「要下级页面」

## 新建 / 改造检查

- [ ] 侧栏预览为单项，无「××列表 / ××详情」子树
- [ ] 详情/编辑通过页内面板、主从或同页替换打开，返回不离开本原型
- [ ] 未新增无必要的 `defineHashPageRoute([...多个 page])`
